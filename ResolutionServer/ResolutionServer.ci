//ResolutionServer.ci

mainmodule ResolutionServer {
	
	extern module Reductions;
	
	readonly int verbosity;

	mainchare Main {
		entry Main();
		entry void nextPart(CkReductionMsg* m);	
	};
	
	array [1D] Worker {
		entry Worker();
		
		entry void readParticles(const std::string& posfilename, const std::string& valuefilename, bool logarithmic, bool reversed, const CkCallback& cb);
		
		entry void generateImage(liveVizRequestMsg* m);
	};
};
